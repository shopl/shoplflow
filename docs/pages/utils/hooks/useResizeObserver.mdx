import {Callout, Tab, Tabs, Pre, Tr, Th, Td, Code, Card, Cards, Table, FileTree, Mermaid, Steps, CopyToClipboard, Button } from "nextra/components";

# useResizeObserver

ResizeObserver를 사용하여 element의 크기를 감지하는 hook입니다.
trackWidth, trackHeight 옵션을 통해 감지할 값을 설정할 수 있습니다.

