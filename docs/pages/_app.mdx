import '@shoplflow/base/styles'
import {useDomain} from "@shoplflow/base";

export default function App({ Component, pageProps }) {
    useDomain('SHOPL');
    return (
        <Component {...pageProps} />
    )
}
