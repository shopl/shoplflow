import { Markdown, Meta } from '@storybook/addon-docs/blocks';
import VERSIONING from '../../VERSIONING.md?raw';

<Meta title="Versioning" />

# Versioning

`@shoplflow/base` 패키지 컴포넌트별 버전 정보입니다.  
아래 내용은 루트의 `VERSIONING.md`를 그대로 표시합니다.

<Markdown>{VERSIONING}</Markdown>
