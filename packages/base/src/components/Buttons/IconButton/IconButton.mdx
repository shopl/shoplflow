import {Controls, Canvas, Meta} from '@storybook/blocks';
import * as IconButtonStories from "./IconButton.stories";

<Meta of={IconButtonStories} />

# IconButton
아이콘 버튼을 만들 때 사용합니다.

> - Icon 컴포넌트와 같이 사용해주세요.
> - as 속성을 이용해 HTML 태그를 변경할 수 있어요. (기본값: button)


## Overview
<Canvas of={IconButtonStories.Solid} />

## Usage
```tsx
import { IconButton } from '@shoplflow/base';

const Example = () => {
  return (
     <IconButton sizeVar={'S'}>
        <Icon iconSource={ViewOffIcon} />
     </IconButton>
  );
};
```

## Props
<Controls of={IconButtonStories.Solid} />
