import { ArgsTable, Canvas, Meta } from '@storybook/blocks';
import Icon from './Icon';
import * as IconStories from './Icon.stories';

<Meta of={IconStories} />

# Icon

Icon Component를 레핑하는 컴포넌트입니다. 각 사이즈에 따라 아이콘 크기를 변경할 수 있습니다.


## ArgsTable

<ArgsTable of={Icon} />

## Usage


<Canvas of={IconStories.Playground} />

`;