import {ArgsTable, Canvas, Meta} from '@storybook/blocks';
import Button from './Button';
import * as ButtonStories from "./Button.stories";

<Meta of={ButtonStories} />

# Button

디자인 시스템에서 사용하는  버튼 컴포넌트에요.

> as 속성을 이용해 HTML 태그를 변경할 수 있어요. (기본값: button)

## Overview

<Canvas of={ButtonStories.AllTexts} />

## Usage

```tsx
import { Button, Typography } from '@hada/ui-kit';

const Example = () => {
  return <Button typograghy={'heading1_700'}>텍스트</Button>;
};
```

## Props

<ArgsTable of={Text} />
