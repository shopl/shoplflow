import * as ChipButtonStories from './ChipButton.stories';
import {Controls, Canvas, Meta} from '@storybook/blocks';
import ChipButton from "./ChipButton";

<Meta of={ChipButtonStories} />

# ChipButton
ChipToggle과 다르게 클릭 이벤트만 가지고 있는 버튼입니다.

> - border-color: **기준이 되는 컬러 (color props로 설정됩니다.)**
> - hover 상태 border-color: border 기준 2단계 높은 색상

## Overview
<Canvas of={ChipButtonStories.Playground} />

## Usage
```tsx
import ChipButton from './ChipButton';

<ChipButton
  text="ChipButton"
  onClick={() => {}}
/>
```

## Props
<Controls of={ChipButtonStories.Playground} />
