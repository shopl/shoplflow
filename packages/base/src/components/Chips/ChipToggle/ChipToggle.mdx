import { Controls, Canvas, Meta } from '@storybook/addon-docs/blocks';
import ChipToggle from './ChipToggle';
import * as ChipToggleStories from './ChipToggle.stories';


<Meta of={ChipToggleStories} />

# ChipToggle
ChipToggle 컴포넌트는 Chip모양의 Toggle입니다.

> - `default`, `hover` 상태의 커스텀은 불가능하며 `selected` 상태의 메인`color`를 선택할 수 있습니다.

## Overview
<Canvas of={ChipToggleStories.Playground} />

## Usage
```tsx
import ChipToggle from './ChipToggle';

<ChipToggle
  text="ChipToggle"
  selected={true}
  onClick={() => {}}
/>
```

## Props
<Controls of={ChipToggleStories.Playground} />
