import { ArgsTable, Canvas, Meta } from '@storybook/blocks';
import List from './List';
import * as ListStories from './List.stories';
import AnatomyList from '../../assets/anatomy/list.png';

<Meta of={ListStories} />

# List

리스트는 2가지 컴포넌트로 구성되어 있습니다.

- List
- List.Text2Rows;


<img src={AnatomyList} alt={'anatomy-list'}/>

<br/>
<br/>

#### 1. leftSource 체크박스, 라디오버튼 등의 왼쪽에 위치하는 컴포넌트를 표현합니다.

#### 2. children 리스트 내부에 들어가는 컴포넌트를 표현합니다. Text2Rows나 Avatar 컴포넌트가 들어갈 수 있습니다.

#### 3. rightSource 리스트 오른쪽에 위치하는 컴포넌트를 표현합니다.

<Canvas of={ListStories.Playground} />


## ArgsTable

<ArgsTable of={List} />

## Usage

<Canvas of={ListStories.WithButton} />

