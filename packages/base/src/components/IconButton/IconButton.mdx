import {ArgsTable, ArgTypes, Canvas, Meta} from '@storybook/blocks';
import IconButton from './IconButton';
import * as IconButtonStories from "./IconButton.stories";

<Meta of={IconButtonStories} />

# IconButton

아이콘을 담고 있는 컴포넌트에요.
> as 속성을 이용해 HTML 태그를 변경할 수 있어요. (기본값: button)

## Overview
<Canvas of={IconButtonStories.Solid} />

## Usage

```tsx
import { IconButton} from '@shoplflow/base';

const Example = () => {
  return <IconButton styleVar="solid" sizeVar="m">아이콘을 넣어주세요.</IconButton>;
};
```

## Props

<ArgTypes of={IconButtonStories} />
