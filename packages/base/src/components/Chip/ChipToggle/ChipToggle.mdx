import { ArgsTable, Canvas, Meta } from '@storybook/blocks';
import ChipToggle from './ChipToggle';
import * as ChipToggleStories from './ChipToggle.stories';


<Meta of={ChipToggleStories} />

# ChipToggle

ChipToggle 컴포넌트는 Chip모양의 Toggle입니다.

`default`, `hover` 상태의 커스텀은 불가능하며 `selected` 상태일 때의 메인`color`를 선택할 수 있습니다.

Chip은 기본적으로 SOLID 스타일이며 각 variants에 따라 스타일이 변경될 수 있습니다.

<Canvas of={ChipToggleStories.Playground} />

## ArgsTable

<ArgsTable of={ChipToggle} />

## Usage

```
import ChipToggle from './ChipToggle';

<ChipToggle
  text="ChipToggle"
  selected={true}
  onClick={() => {}}
/>
```

