import {ArgsTable, Meta} from '@storybook/blocks';


import * as ModalStories from "./Modal.stories";
import {Modal} from "./index";


<Meta of={ModalStories} />

# Modal

Stack 컴포넌트는 자식 컴포넌트의 레이아웃 영역을 감싸는 용도로 사용해요.


<ArgsTable of={Modal.Container} />
