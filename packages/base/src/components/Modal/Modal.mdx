import {ArgsTable, Meta, Canvas} from '@storybook/blocks';
import {Modal} from "./index";

import * as ModalStories from "./Modal.stories";

<Meta of={ModalStories} />

# Modal

Modal 컴포넌트는 Window 위에 추가적으로 나타나는 팝업입니다.

Modal 컴포넌트는 4가지 컴포넌트로 구성되어있습니다.

- Modal.Container (Modal 컴포넌트의 최상위 컴포넌트)
- Modal.Header (Modal 컴포넌트의 헤더 컴포넌트)
- Modal.Body (Modal 컴포넌트의 바디 컴포넌트)
- Modal.Footer (Modal 컴포넌트의 푸터 컴포넌트)

> **아래의 조건으로 스타일이 변경됩니다.**
- Header가 있는 경우: Body 상단 padding 16px
- Header가 없는 경우: Body 상단 padding 24px

> **스크롤바 컴포넌트를 사용하여 기본 스크롤바가 나타나지 않습니다.**

- `react-custom-scrollbars-2`라이브러리를 사용 중이며 번들 결과물에 포함됩니다.


# ArgsTable
---

### Modal.Container

<ArgsTable of={Modal.Container} />

# Usage

<Canvas of={ModalStories.Primary} />

<Canvas of={ModalStories.HeaderBody} />
<Canvas of={ModalStories.BodyFooter} />


