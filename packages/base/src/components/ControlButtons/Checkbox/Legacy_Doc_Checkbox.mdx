import { Controls, Canvas, Meta } from '@storybook/addon-docs/blocks';
import Checkbox from './Checkbox';
import * as CheckboxStories from './Checkbox.stories';

<Meta of={CheckboxStories} />

# Checkbox
선택할 수 있는 옵션을 제공하는 버튼 컴포넌트입니다.
> - `<input>`의 기본적인 속성들을 사용할 수 있습니다.

## Overview
<Canvas of={CheckboxStories.Playground} />

## Usage
```tsx
import { Checkbox } from '@shoplflow/base';

const App = () => {
  return (
      <Checkbox onClick={()=>{console.log('click');}}/>
  );
};
```

## Props
<Controls of={CheckboxStories.Playground} />