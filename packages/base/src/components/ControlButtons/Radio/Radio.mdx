import { Controls, Canvas, Meta } from '@storybook/addon-docs/blocks';
import Radio from './Radio';
import * as RadioStories from './Radio.stories';

<Meta of={RadioStories} />

# Radio
값을 단건으로 선택할 때 사용하는 버튼 컴포넌트입니다.
> - `<button>`의 기본적인 속성들을 사용할 수 있습니다.

## Overview
<Canvas of={RadioStories.Playground} />

## Usage
```tsx
import { Radio } from '@shoplflow/base';

const App = () => {

  return (
      <Radio/>
  );
};
```

## Props
<Controls of={RadioStories.Playground} />
