import { Canvas, ArgsTable, Meta } from '@storybook/blocks';
import * as ToggleStories from './Toggle.stories';
import Toggle from './Toggle';

<Meta of={ToggleStories}/>

# Toggle

정해진 옵션값을 조작하는 버튼입니다. <br/>
default, active, disabled의 상태를 가지며 `activeColor` 속성으로 활성화 상태의 배경 색상을 변경할 수 있습니다.

 <Canvas of={ToggleStories.Playground}/>

## Usage

```tsx
import Toggle from "./Toggle";

const Example = () => {
    return (
        <Toggle/>
    )
}
```

<ArgsTable of={Toggle}/>