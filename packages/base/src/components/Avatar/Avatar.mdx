import { Canvas, Controls, Story, Meta } from '@storybook/blocks';
import Avatar from './Avatar';
import * as AvatarStories from './Avatar.stories';

<Meta of={AvatarStories} />

# Avatar
Avatar는 사용자의 프로필 이미지를 표시하는 컴포넌트에요.

## Overview
<Canvas of={AvatarStories.Playground} />

## Usage
```tsx
import { Avatar } from '@shoplflow/base';

<Avatar badge={<Icon sizeVar={'XS'} iconSource={LeaderLargeIcon} />} {...args} />
```

## Props
<Controls of={AvatarStories.Playground} />




