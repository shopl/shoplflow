import { ArgsTable, Canvas, Meta } from '@storybook/blocks';
import Avatar from './Avatar';
import * as AvatarStories from './Avatar.stories';

<Meta of={AvatarStories} />

# Avatar

Avatar 컴포넌트 설명을 적어주세요.

<Canvas of={AvatarStories.Playground} />


## ArgsTable

<ArgsTable of={Avatar} />

## Usage

```tsx
const App = () => (
  return <Avatar badge={<Icon sizeVar={'XS'} iconSource={LeaderLargeIcon} />} src={'url'}/>
);
```


