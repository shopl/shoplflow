import { ArgsTable, Canvas, Meta } from '@storybook/blocks';
import FilterSkeleton from './FilterSkeleton';
import * as FilterSkeletonStories from './FilterSkeleton.stories';
import { Markdown } from '@storybook/blocks';
import ReadMe from './FilterSkeleton.md?raw';

<Meta of={FilterSkeletonStories} />

# FilterSkeleton

`FilterSkeleton`은 로딩 상태를 보여주기 위한 컴포넌트입니다. 주로 필터와 관련된 UI에서 데이터를 로드하는 동안 사용할 수 있습니다.

<Canvas of={FilterSkeletonStories.Playground} />

## ArgsTable

<ArgsTable of={FilterSkeleton} />

---

## Usage

<Markdown>{ReadMe}</Markdown>