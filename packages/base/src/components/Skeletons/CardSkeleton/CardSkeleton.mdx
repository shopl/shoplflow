import { ArgsTable, Canvas, Meta } from '@storybook/blocks';
import CardSkeleton from './CardSkeleton';
import * as CardSkeletonStories from './CardSkeleton.stories';
import { Markdown } from '@storybook/blocks';
import ReadMe from './CardSkeleton.md?raw';

<Meta of={CardSkeletonStories} />

# CardSkeleton

카드 형태의 콘텐츠가 로딩 중일 때 표시되는 로딩 상태 컴포넌트입니다.

## Overview

- 헤더 영역에는 원형 아바타와 제목을 나타내는 스켈레톤이 표시됩니다
- 컨텐츠 영역에는 4개의 카드 형태 스켈레톤이 표시됩니다(고정)

<Canvas of={CardSkeletonStories.Playground} />


## Usage

<Markdown>{ReadMe}</Markdown>