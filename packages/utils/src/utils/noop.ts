/**
 * 아무 일도 하지 않는 함수입니다.
 *
 * props에 기본값을 전달하기 위한 용도로 사용됩니다.
 *
 * ## Usage
 * ```tsx
 * import {noop} from "@shoplflow/utils"
 * const Component = ({onClick = noop}) => {
 * ```
 */
// eslint-disable-next-line @typescript-eslint/no-empty-function
export const noop = function () {};
